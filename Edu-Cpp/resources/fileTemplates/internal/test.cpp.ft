\#include <gtest/gtest.h>
\#include <string>

// Headers of objects that student should realize:
std::string task();


// Tests:
TEST(TestSuiteName, TestName) {
    ASSERT_EQ(task(), "Expected answer");
}