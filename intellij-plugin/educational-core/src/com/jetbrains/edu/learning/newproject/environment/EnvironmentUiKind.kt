package com.jetbrains.edu.learning.newproject.environment

/**
 * The environments might be selected from a list or not selected at all because there is only one meaningful environment.
 */
enum class EnvironmentUiKind {
  ComboBox,
  Empty
}
